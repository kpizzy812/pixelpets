
# 1. Общая концепция

**Проект:** Pixel Pets
**Формат:** Crypto NFT-like экономическая игра в Telegram Mini App.
**Суть:**
Игрок покупает пиксельных петов за внутренний токен **XPET** (1 XPET = $1), отправляет их на тренировку, фармит % в день до достижения капы. Можно:

* покупать петов разных тарифов,
* прокачивать (додепать через уровни Baby → Teen → Adult → Mythic),
* фармить доход через ежедневные тренировки,
* досрочно “продавать” пета со штрафом,
* получать реферальный доход с claim партнёров,
* выполнять задания и получать XPET.

Экономика: **чистый ponzi + внешнее донатное финансирование с белого проекта** (логика на бэке, пользователям можно подавать как “игровой лут”).

---

# 2. Валюта и балансы

### 2.1. XPET

* Основная и единственная внутренняя единица: **XPET**.
* **Фикс:** 1 XPET = 1 USD (на старте, можно в конфиге).
* Все цены, депозиты, доходы, задания — в **XPET**.

### 2.2. Отображение в интерфейсе

* **Главный баланс**:

  * Хранится как число XPET.
  * На экране Home показывается как `$XXX.XX` (форматирование в $).
* **Магазин / петы / задания**:

  * Цены, награды — в XPET (цифра с иконкой XPET).

### 2.3. Депозиты / вывод

* **Пополнение**:

  * Минимум: **5 XPET**.
  * Способы: USDT по сетям **BEP-20, Solana, TON**.
  * Процесс:

    1. Игрок создаёт заявку на пополнение (выбирает сеть, сумму).
    2. Получает адрес/QR.
    3. Отправляет USDT.
    4. Админ в админке подтверждает → начисляет XPET 1:1.
* **Вывод**:

  * Минимум: **5 XPET**.
  * Комиссия: **$1 + 2%** от суммы.
  * Процесс:

    1. Игрок создаёт заявку на вывод (сеть, адрес, сумма).
    2. Сумма списывается с баланса XPET (с учётом комиссии).
    3. Админ исполняет вручную и помечает заявку выполненной.

---

# 3. Петы / тарифы

## 3.1. Типы петов

Каждый тип пета — это тарифный план.

| ID | Название     | Emoji | Базовая цена (XPET) | Дневной % | Капа ROI от инвеста |
| -- | ------------ | ----- | ------------------- | --------- | ------------------- |
| 1  | Bubble Slime | 🫧    | 5                   | 1.0%      | 150% (1.5x)         |
| 2  | Pixel Fox    | 🦊    | 50                  | 1.2%      | 160% (1.6x)         |
| 3  | Glitch Cat   | 🐱    | 100                 | 1.5%      | 170% (1.7x)         |
| 4  | Robo-Bunny   | 🤖    | 150                 | 2.0%      | 180% (1.8x)         |
| 5  | Ember Dragon | 🐉    | 300                 | 2.5%      | 200% (2.0x)         |

**Интерпретация:**

* Дневной % применяется к **текущей инвестированной сумме** в пета (включая апгрейды).
* Капа = максимальный суммарный **профит**, который пет может принести, как **процент от текущего тела инвестиций** (с учётом всех апгрейдов).

---

# 4. Уровни и апгрейды петов

## 4.1. Уровни

У каждого пета есть уровни, привязанные к сумме инвестиций:

* **Baby**
* **Adult**
* **Mythic**

Количество уровней: **3** (задаётся конфигом).

## 4.2. Механика апгрейда

* При покупке пета он создаётся на **уровне Baby** с **базовой ценой**.
* Апгрейд осуществляется вручную:

  * Кнопка **Upgrade** (иконка стрелка вверх) на карточке пета.
  * При апгрейде списывается XPET с баланса и увеличивается `invested_total` пета.
  * Пересчитывается:

    * уровень (`Baby → Teen → Adult → Mythic`),
    * новая капа, т.к. зависит от `invested_total`.

### 4.3. Примерный лестничный апгрейд (рекомендуемое ТЗ)

*(Цифры можно потом подогнать, но логика именно такая)*

Для каждого типа пета в конфиге задаются:

* `base_price` — стартовая цена.
* `max_price` — максимальная целевая цена (из твоих диапазонов).
* `levels` — массив целевых сумм по уровням.

Пример (Bubble Slime):

* base: 5 XPET (Baby)
* Adult: 15 XPET
* Mythic: 30 XPET

Игрок видит:

* текущий уровень,
* текущую общую стоимость пета (invested_total),
* стоимость апгрейда до следующего уровня: `next_level_price - invested_total`.

**Важно:** капа всегда считается от **текущего `invested_total`**, а не от base.

---

# 5. ROI капа и формулы доходов

## 5.1. Переменные

Для каждого пета `pet`:

* `invested_total` — общая сумма вложений в пета (XPET).
* `daily_rate` — дневной процент (0.01–0.025).
* `roi_cap_multiplier` — капа (1.5–2.0).
* `profit_claimed` — сколько профита уже заклеймил этот пет (XPET).

## 5.2. Максимальный профит (кап)

```text
max_profit = invested_total * roi_cap_multiplier
```

Пет может принести **суммарно** максимум `max_profit`.
Когда `profit_claimed >= max_profit` → пет **эволвировал и завершён**.

## 5.3. Доход за тренировку (Claim)

Тренировка = 24 часа.

```text
daily_profit_raw = invested_total * daily_rate
remaining_profit = max_profit - profit_claimed
profit_for_claim = min(daily_profit_raw, remaining_profit)
```

* Если `profit_for_claim > 0`:

  * на баланс игрока добавляется `profit_for_claim`.
  * `profit_claimed += profit_for_claim`.
* Если после клейма `profit_claimed >= max_profit`:

  * пет переходит в статус **Evolved** (Hall of Fame),
  * больше не может тренироваться.

---

# 6. Состояния пета и тренировка

## 6.1. Стейты пета

Для каждого экземпляра пета:

* `LOCKED` — ещё не куплен (только в магазине).
* `OWNED_IDLE` — куплен, не в тренировке, готов к запуску.
* `TRAINING` — идёт 24h тренировка.
* `READY_TO_CLAIM` — тренировка завершена, можно нажать Claim.
* `EVOLVED` — достигнут ROI cap, пет завершён (в Hall of Fame).
* `SOLD` — пет досрочно продан.

## 6.2. Тренировка

* Запуск тренировки: кнопка **Start** (иконка, например ⚡ / ▶️) на карточке пета.
* Для каждого пета индивидуальный таймер на **24 часа**:

  * хранится `training_started_at`.
  * `training_ends_at = training_started_at + 24h`.

### Логика по времени

* Фронт периодически проверяет время (локально) и состояние:

  * до `training_ends_at` → статус TRAINING, показываем оставшийся таймер.
  * после `training_ends_at` → статус READY_TO_CLAIM, кнопка Claim.

### Важно:

* Пока игрок **не нажал Claim**, новый цикл тренировки **не начинается**.
* Это фикс: `READY_TO_CLAIM` — тупик до клейма или продажи.

---

# 7. Claim

Когда пользователь нажимает **Claim**:

1. Бек проверяет:

   * статус пета = `READY_TO_CLAIM`,
   * время ≥ `training_ends_at`.
2. Считает профит по формуле выше.
3. Увеличивает:

   * `user_balance_xpet += profit_for_claim`,
   * `pet.profit_claimed += profit_for_claim`.
4. Обновляет статус:

   * если `profit_claimed >= max_profit` → `EVOLVED` (Hall of Fame),
   * иначе → `OWNED_IDLE` (готов к новой тренировке).
5. Триггерит реферальные начисления (см. ниже).

---

# 8. Досрочная продажа пета

## 8.1. Механика

Пользователь может **продать пета** до достижения капы.

* Доступно из стейтов:

  * `OWNED_IDLE`
  * `READY_TO_CLAIM`
  * `TRAINING` *(можно, если хочешь жёсткости — можно запретить; но по ТЗ пока не запрещаем)*.

## 8.2. Что получает игрок

* Возвращается **только тело депозита** `invested_total`,
* Минус **15% комиссии**.

```text
refund = invested_total * 0.85
user_balance_xpet += refund
```

* Любой **не заклеймленный профит сгорает**:

  * если пет в `READY_TO_CLAIM` или `TRAINING`, профит за текущий цикл игрок не получает.
  * логически можно учитывать как `profit_burned`, но это для статистики.

Пет получает статус `SOLD`. Слот освобождается.

---

# 9. Слоты и лимиты

* У каждого игрока: **3 слота** под активных петов.
* В один слот — **ровно один экземпляр пета** (определённого типа и уровня).
* Можно иметь несколько одинаковых типов петов (например, 3 Bubble Slime), пока хватает слотов.
* При:

  * продаже пета,
  * эволюции до капы (EVOLVED и уходит в Hall of Fame)
    → слот освобождается автоматически.

---

# 10. Реферальная система

## 10.1. Структура

* До **5 уровней** в глубину.
* Рефка начисляется **с каждого Claim** реферала (то есть с профита, а не депозита).

## 10.2. Проценты

Уровни:

1. Уровень 1: **20%** от профита на клейме.
2. Уровень 2: **15%**
3. Уровень 3: **10%**
4. Уровень 4: **5%**
5. Уровень 5: **2%**

```text
ref_bonus_k = profit_for_claim * ref_percent_k
```

* Если на каком-то уровне нет партнёра — рефка просто не начисляется.

## 10.3. Открытие уровней

* Уровни 2–5 не открыты сразу.
* Условия открытия (в конфиге, пример):

  * L1 — открыт сразу.
  * L2 — от 3 активных рефералов (которые купили хотя бы 1 пета).
  * L3 — от 5 активных.
  * L4 — от 10 активных.
  * L5 — от 20 активных.
* Это можно оставить как конфиг для админки: `min_active_refs_for_level_k`.

## 10.4. Начисление

* Вся рефка начисляется **в XPET**.
* Добавляется сразу на **баланс пользователя**.
* Можно сразу тратить на покупку/апгрейд или выводить.

---

# 11. Задания (Tasks)

## 11.1. Типы заданий (первый этап)

* Подписка на Telegram-канал / чат.
* Другие типы можно добавить позже (Twitter, Discord и т.д.).

## 11.2. Механика

Для каждого задания:

* `title` — краткое название.
* `description` — (минимальный текст, можно для админки).
* `reward_xpet` — 0.1–0.5 XPET (мелкие, “копеечные” награды).
* `link` — URL/Deep Link (например, tg://resolve?domain=...).
* `is_active` — флаг активности.
* `order` — порядок в списке.

### Флоу для игрока:

1. На вкладке **Tasks** видит список активных задач.
2. Жмёт на задачу → открывается внешний ресурс (канал, чат).
3. Возвращается в мини-апп → жмёт кнопку **“Проверить”**.
4. Бэкенд:

   * проверяет условие (например, подписан ли на TG-канал, либо пока просто “по кнопке”).
   * если успешно — задача помечается как **Completed** для этого игрока.
   * начисляет `reward_xpet` на баланс.
5. Завершённые задачи переваливаются вниз списка (или в отдельный блок “Completed”).

## 11.3. Админка для задач

* Создать задачу.
* Редактировать:

  * название,
  * текст,
  * награду,
  * ссылку,
  * флаг активности.
* Скрывать (дизейбл) без удаления.

---

# 12. UI / Страницы мини-аппа

Мини-апп: **Next.js** + Telegram Mini App SDK.

## 12.1. Навигация (табар)

Внизу таббар:

1. **Home** (иконка домика)
2. **Shop** (иконка витрины/карточки)
3. **Tasks** (иконка чек-листа)
4. **Referrals** (иконка людей/сеть)

В хедере Home:

* Баланс (в $),
* Кнопка кошелька (депозит/вывод),
* Кнопка настроек (иконка шестерёнки).

---

## 12.2. Экран Home

Элементы:

1. **Баланс**:

   * Большая цифра: `$123.45`.
   * Под ней мелко: `XPET: 123.45`.
   * Кнопка кошелька (иконка):

     * при нажатии открывается экран Wallet (пополнение/вывод).

2. **Список петов (3 слота)**:

   * Каждый слот — карточка.
   * Если слот пуст:

     * картинка “пустой слот”,
     * CTA: “Buy pet” → ведёт в Shop (по UX — это будет иконка + короткий текст).

3. **Карточка пета (когда слот занят)**

**Состав карточки:**

* Аватар пета (пиксель арт).

* **Полоса уровня** с кнопкой апгрейда:

  * Progress bar с подписью уровней: Baby / Teen / Adult / Mythic,
  * Иконка стрелка вверх (Upgrade) справа.

* **Полоса редкости**:

  * Можно просто стильная полоска с подсветкой по редкости (Common, Rare, Epic, Legendary/Mythic).

* Текстовые параметры:

  * Стоимость пета: `Invested: 150 XPET` (иконка монеты вместо слова).
  * Процент: `Daily: 2.0%`.

* **Статус/кнопка**:

  * В зависимости от стейта:

    * `OWNED_IDLE` → кнопка **Start** (иконка ▶️/⚡).
    * `TRAINING` → таймер (обратный отсчёт 24h, иконка ⏱).
    * `READY_TO_CLAIM` → кнопка **Claim** (иконка мешка/стрелка вниз на кошелёк).
    * `EVOLVED` → бейдж “Evolved”/“Hall of Fame”.
    * `SOLD` → слот уже пуст, пета нет.

* Отдельно (иконка корзины/крестика) — кнопка **Sell** (досрочная продажа) с подтверждением через модалку.

---

## 12.3. Экран Shop

Задача: показать список доступных петов для покупки.

Для каждого типа пета:

* Аватар + название (emoji + имя).
* Редкость (цветная полоска / тэг).
* Базовая цена: `5 XPET`.
* Дневной %: `1.0% daily`.
* Капа: `150% ROI cap`.
* Кнопка **Buy**:

  * Если есть свободный слот → покупка.
  * Если слоты заняты → показывать предупреждение (нет свободных слотов).

При покупке:

* модалка: подтверждение списания XPET.
* если баланс < стоимости → ошибка “Not enough XPET”.

---

## 12.4. Экран Tasks

* Список активных задач.
* Для каждой:

  * Иконка (TG-логотип и т.п.),
  * Короткий заголовок,
  * Награда: `+0.3 XPET`,
  * Кнопка:

    * “Go” → открывает ссылку,
    * после возвращения кнопка меняется на “Check”.
* Completed задачи:

  * Отмечены галочкой,
  * Могут быть внизу списка/в отдельном блоке.

---

## 12.5. Экран Referrals

* Блок “Пригласи друзей”:

  * Реф-ссылка / реф-код.
  * Кнопка “Copy / Share”.
* Статистика:

  * Сколько заработано на рефке (XPET / $),
  * Количество активных рефералов,
  * Уровни (1–5) с прогрессом открытия (например, замочек / прогрессбар).
* Список рефералов (опционально, минимум: количество по уровням).

---

## 12.6. Экран Wallet (кошелёк)

* Баланс в XPET и $.
* Кнопка **Deposit**:

  * выбор сети: BEP-20 / Sol / TON,
  * ввод суммы,
  * показ адреса/QR,
  * статус заявки: Pending / Confirmed.
* Кнопка **Withdraw**:

  * ввод суммы (min 5),
  * показ комиссии `1 + 2%`,
  * ввод адреса,
  * отправка заявки.

---

# 13. Бэкенд (FastAPI) — структура

Краткий набросок эндпоинтов (без кода):

### Auth / User

* `POST /auth/telegram` — авторизация по Telegram initData.
* `GET /me` — профиль, баланс, реф-код, реф-стат.

### Pets

* `GET /pets/catalog` — список типов петов (тарифы).
* `GET /pets/my` — список петов пользователя (3 слота + их стейты).
* `POST /pets/buy` — покупка пета.
* `POST /pets/upgrade` — апгрейд пета.
* `POST /pets/start-training` — запуск тренировки.
* `POST /pets/claim` — клейм профита.
* `POST /pets/sell` — досрочная продажа.

### Wallet

* `GET /wallet` — баланс XPET, в $.
* `POST /wallet/deposit-request` — создать заявку на пополнение.
* `POST /wallet/withdraw-request` — заявка на вывод.
* `GET /wallet/transactions` — история.

### Referrals

* `GET /referrals` — данные по структуре, доходу, уровням.
* `GET /referrals/link` — реф-ссылка/код.

### Tasks

* `GET /tasks` — список активных задач + completed флаги.
* `POST /tasks/check` — проверка выполнения задачи, начисление награды.

---

# 14. Модели данных (в общих чертах)

### User

* `id`
* `telegram_id`
* `balance_xpet` (Decimal/int)
* `ref_code`
* `referrer_id`
* `ref_levels_unlocked` (1–5)
* `created_at`

### PetType

* `id`
* `name`
* `emoji`
* `base_price`
* `daily_rate`
* `roi_cap_multiplier`
* `level_prices` (JSON: Baby/Teen/Adult/Mythic суммы)

### UserPet

* `id`
* `user_id`
* `pet_type_id`
* `invested_total`
* `level` (Baby/Teen/Adult/Mythic)
* `status` (OWNED_IDLE/TRAINING/READY_TO_CLAIM/EVOLVED/SOLD)
* `training_started_at`
* `training_ends_at`
* `profit_claimed`
* `slot_index` (0–2)
* `created_at`

### Transactions

* `id`
* `user_id`
* `type`: deposit / withdraw / claim / ref / task_reward / sell_refund / admin_adjust
* `amount_xpet`
* `meta` (JSON)
* `created_at`

### ReferralStats

* `user_id`
* `level_1_count`, ...
* `total_ref_earned_xpet`

### Task

* `id`
* `title`
* `description`
* `reward_xpet`
* `link`
* `is_active`
* `order`

### UserTask

* `id`
* `user_id`
* `task_id`
* `status` (pending/completed)
* `completed_at`

---

# 15. Админка (минимум, который нужен)

1. **Пользователи:**

   * поиск по telegram_id / ref_code,
   * просмотр баланса, петов, реф-дохода,
   * ручная корректировка баланса (adj).

2. **Депозиты / выводы:**

   * список заявок,
   * фильтрация по статусу,
   * установка статуса: approved/rejected,
   * автоматическое начисление при approve.

3. **Петы и тарифы:**

   * настройка PetType:

     * base_price,
     * daily_rate,
     * roi_cap_multiplier,
     * level_prices.

4. **Реферальные уровни:**

   * проценты по уровням,
   * пороги акт. рефералов для открытия уровней.

5. **Tasks:**

   * список задач,
   * создание/редактирование/скрытие,
   * изменение наград.


Бро, ловлю. Сделаю два мощных блока:

1. **Гейм-лор и короткие игровые тексты (UI-copy)** — стиль минималистичный, мемный, понятный, без воды, чтобы игрок чуть улыбался, но не читал простыни.
2. **Блок про локализацию** — структурно, чтобы сразу можно было добавить EN/RU/EU наборы.

---

# 🐾 **БЛОК 1 — ЛОР И ВСЕ КОРОТКИЕ ИГРОВЫЕ ТЕКСТЫ**

Твой вайб: минималистично, пиксельно-мемно, но без “детского сада”.

## 🔥 Общая легенда (краткая)

Pixel Pets — цифровые пиксельные хищники, которых ты тренируешь, прокачиваешь и отправляешь фармить добычу. Каждый из них живёт **цикл**, после которого он становится слишком сильным для арены и уходит в “Hall of Fame”.

Всё остальное — действия игрока, упакованные в короткие тексты.

---

## 🧬 **1. Покупка пета**

### Легенда

Ты находишь нового пиксельного зверя и забираешь себе.

### UI-тексты

* **Buy Pet** → *“Recruit”*
* Покупка успешна →
  **“New pet recruited.”**
* Нет слотов →
  **“No free slots.”**
* Недостаточно баланса →
  **“Not enough XPET.”**

---

## ⚡️ **2. Тренировка (Start)**

### Легенда

Ты отправляешь питомца на 24h миссию. Он фармит лут (= доход), растёт, готовится к следующей эволюции.

### UI-тексты

* Кнопка запуска → **“Train 24h”**
* После старта →
  **“Training…”**
* Таймер →
  **“Time left:”**
* Tooltip/подпись →
  **“Your pet is on a mission.”**

---

## 📦 **3. Claim (сбор добычи)**

### Легенда

Питомец вернулся с добычей → ты забираешь награду.

### UI-тексты

* Статус → **“Mission complete”**
* Кнопка → **“Claim Loot”**
* Успешно → **“Loot collected.”**
* Ошибка, если рано → **“Mission not finished.”**

---

## 🧪 **4. Апгрейды (Baby → Adult → Mythic)**

### Легенда

Питомец растёт, его ядро усиливается, он получает новый класс и фармит больше.

### UI-тексты

**Название уровней:**

* Baby → **Baby**
* Adult → **Adult**
* Mythic → **Mythic**

**Кнопка:**

* **“Upgrade”**

**Подписи:**

* Успешно → **“Pet upgraded.”**
* Нет баланса → **“Not enough XPET.”**
* Максимальный уровень → **“Max level reached.”**

---

## 🔥 **5. Эволюция после капы (Hall of Fame)**

### Легенда

Питомец достиг своего предела. Он настолько силён, что не может дальше тренироваться — покидает арену и навсегда остаётся в твоём “Зале Славы”.

### UI-тексты

* Финальный статус → **“Evolved”**
* Подпись → **“This pet has reached its full potential.”**
* В Hall of Fame:

  * **“Total loot farmed:”**
  * **“Lifetime:”**
  * **“Final class:”**
* Слот освобождается →
  **“Slot cleared.”**

---

## 💀 **6. Досрочная продажа пета**

### Легенда

Ты отпускаешь пета раньше времени. Он уходит недотренированным → получаешь только часть вложений. Добыча с текущей миссии сгорает.

### UI-тексты

* Кнопка в карточке → **“Release”**
* Модалка подтверждения →
  **“Release this pet early?
  You will get 85% of invested XPET.
  Unclaimed loot will be lost.”**
* Подтверждение → **“Release”**
* Успешно → **“Pet released.”**

---

## 🌐 **7. Задания (Tasks)**

### Легенда

Ты выполняешь простые миссии за маленькие награды (XPET), чтобы бустануть свой путь.

### UI-тексты

* Кнопка → **“Go”**
* Кнопка → **“Check”**
* Успех → **“Completed.”**
* Награда → **“+0.3 XPET”**
* Пустой список → **“No tasks available.”**

---

## 🧲 **8. Рефералка**

### Легенда

Ты строишь пиксельную стаю: каждый друг приносит долю добычи, когда делает Claim.

### UI-тексты

* Заголовок → **“Invite Friends”**
* Ссылка → **“Copy link”**
* Уровни → **“Referral Levels”**
* Открытие уровней → **“Unlock: X active partners”**
* Награды → **“Earned from referrals:”**
* При начислении → **“Referral loot received.”**

---

## 💸 **9. Баланс / пополнение / вывод**

### UI-тексты

* Баланс → **“Balance”**, отображение `$123`
* Пополнение → **“Deposit XPET”**
* Вывод → **“Withdraw”**
* Комиссия → **“Fee: $1 + 2%”**
* Нет суммы → **“Enter amount”**
* Минимум → **“Minimum: 5 XPET”**

---

## 🛠 **10. Ошибки / общие короткие тексты**

* **“Something went wrong.”**
* **“Try again.”**
* **“Not enough XPET.”**
* **“No free slots.”**
* **“Already training.”**
* **“Action unavailable.”**

---

# 🌍 **БЛОК 2 — ЛОКАЛИЗАЦИЯ (EN / RU / EU)**

Ты хочешь масштабировать, значит делаем архитектуру сразу.

## 🎯 Основная идея

Все тексты должны быть в одном объекте (i18n файл),
структурированно → чтобы фронт-программисты могли легко подставлять строки.

---

# 🌐 **1. Языки на старте**

### обязательно:

* **EN**
* **RU**

### EU (минимальный набор):

* **DE — German**
* **ES — Spanish**
* **FR — French**
* **PT — Portuguese**
* **IT — Italian**

*Это покрывает 90% европейской аудитории.*

---

# 📁 **2. Структура i18n файла**

```json
{
  "common": {
    "balance": "...",
    "upgrade": "...",
    "start": "...",
    "claim": "...",
    "release": "...",
    "ok": "...",
    "cancel": "..."
  },
  "pet": {
    "train_24h": "...",
    "training": "...",
    "mission_complete": "...",
    "evolved": "...",
    "upgrade_success": "...",
    "not_enough_xpet": "...",
    "release_confirm": "...",
    "released": "..."
  },
  "shop": {
    "buy_pet": "...",
    "no_slots": "...",
    "not_enough_balance": "...",
    "recruited": "..."
  },
  "tasks": {
    "go": "...",
    "check": "...",
    "completed": "...",
    "reward": "...",
    "no_tasks": "..."
  },
  "ref": {
    "invite": "...",
    "copy": "...",
    "ref_levels": "...",
    "unlocked": "...",
    "ref_reward": "..."
  },
  "wallet": {
    "deposit": "...",
    "withdraw": "...",
    "fee": "...",
    "min": "...",
    "enter_amount": "..."
  }
}
```

---

# 🚀 3. Переключение языка

Mini App может:

* определять язык пользователя по `initData.user.language_code`,
* если язык не поддерживается → выставлять EN,
* сохранять выбранный язык в localStorage,
* менять его в настройках.

В UI — иконка 🌐 → выпадушка языков.

